services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspaces/pdf-form-studio:cached
      - claude-code-config:/home/node/.claude
    environment:
      CLAUDE_CONFIG_DIR: /home/node/.claude
    command: sleep infinity

volumes:
  claude-code-config:
